## Restaurant Reviews App
This is the restaurant reviews application which is working offline too.
### Usage
1. Install dependencies by running `npm install --save` command.
2. Start application by running `gulp` command.